- Document the TableXxx asserts

- Fix AssertTableRowCountEqualTo.ssunit.sql with "and p.index_id in (0, 1)"

- Add option to invoke teardown before setup

- Consider reporting a TearDown failure before an assert failure

- Add a result set comparison assert

- Allow the _@Test@_ attributes to appear anywhere (so the test name leads)

- AssertTableRowCountEqualTo doesn't mention table name in the error.

- Run a single test in SSMS

- AssertXxxIsNotNull

- Asserts - Date only

- Asserts - DateTime needs a tolerance (minutes?) or DateTimeBetween

- AssertThrowsAnyError overload
